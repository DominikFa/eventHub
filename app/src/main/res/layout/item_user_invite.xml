<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp"
    android:background="?attr/selectableItemBackground">

    <ImageView
        android:id="@+id/iv_user_avatar_invite_item"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/ic_profile_placeholder"
        android:contentDescription="@string/content_desc_user_avatar"
        android:background="@drawable/avatar_background_shape"
        android:padding="2dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <TextView
        android:id="@+id/tv_user_name_invite_item"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="8dp"
        android:textAppearance="?attr/textAppearanceSubtitle1"
        android:textColor="@color/event_hub_text_primary_profile"
        android:textSize="16sp"
        app:layout_constraintStart_toEndOf="@id/iv_user_avatar_invite_item"
        app:layout_constraintEnd_toStartOf="@+id/checkbox_invite_user"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="User Full Name" />

    <TextView
        android:id="@+id/tv_user_login_invite_item"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="8dp"
        android:textAppearance="?attr/textAppearanceBody2"
        android:textColor="@color/event_hub_text_label_profile"
        android:textSize="14sp"
        app:layout_constraintStart_toEndOf="@id/iv_user_avatar_invite_item"
        app:layout_constraintEnd_toStartOf="@+id/checkbox_invite_user"
        app:layout_constraintTop_toBottomOf="@id/tv_user_name_invite_item"
        tools:text="username123" />

    <CheckBox
        android:id="@+id/checkbox_invite_user"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
